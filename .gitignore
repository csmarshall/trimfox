# macOS
.DS_Store

# session / working state (never committed)
session-state.md

# installer backups left in a profile, and logs
*.bak-*
*.log

# personal, per-user overrides — never committed (each user owns their own)
chrome/user-overrides.css
user-overrides.js
dist/
